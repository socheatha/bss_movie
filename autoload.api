<?php 
	define('BASE_PATH', __DIR__. '/');
	define('ABSOLUTE_PATH', '/');
	define('DS', DIRECTORY_SEPARATOR);

	foreach (['config/connection.api', 'api/general.api'] as $url) {
		if ($url) require_once BASE_PATH . DS . $url;
	}